package ru.job4j.map;

import ru.job4j.simpleArray.Set;

import java.util.HashMap;
import java.util.Map;

public class Model {
    private Map<User, Email> table = new HashMap<>();

    public boolean add(User user, Email email){
        boolean rsl = false;
        for (Email e : table.
             ) {

        }
    }
}
